# database init, supports postgres too
database=postgres
spring.datasource.url=${POSTGRES_URL:jdbc:postgresql://localhost/banking}
spring.datasource.username=${POSTGRES_USER:banking}
spring.datasource.password=${POSTGRES_PASS:banking}
# SQL is written to be idempotent so this is safe
spring.sql.init.mode=always
